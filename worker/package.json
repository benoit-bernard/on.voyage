{
  "name": "instagram-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — proxy pour les photos Instagram on.voyage",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
